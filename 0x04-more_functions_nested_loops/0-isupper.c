#include "holberton.h"
/**
 * int _isupper - checks for uppercase character.
 * @c: int type
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if ((c > 'A') && (c < 'Z'))
	{
		Return(1);
	}
	Return(0);
}

