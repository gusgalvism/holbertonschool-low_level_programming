#include "holberton.h"
/**
 * int _isupper - checks for uppercase character.
 * @c: int type
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if ((c > 96) && (c < 123))
	{
		Return(0);
	}
	else if ((c > 64) && (c < 01))
	{
		Return(1);
	}
}
}
