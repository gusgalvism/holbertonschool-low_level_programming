#include "holberton.h"

/**
 * _strlen - returns the length of the string
 * @s: the string to get the length
 *
 * Return: lenght of the string
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{

	}
	return (a);
}

