#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */

int print_last_digit(int n)
{
	int a;

	if (b >= 0)
		a = b % 10;
	else
		a = (b % 10) * -1;
	_putchar(a + '0');
	return (a);
}